{
  "mcpServers": {
    "youtrack": {
      "command": "uv",
      "args": [
        "run",
        "--with", "mcp",
        "--with", "keyring",
        "${CLAUDE_PLUGIN_ROOT}/servers/youtrack_server.py"
      ]
    }
  }
}
